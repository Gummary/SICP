(define (adjoin-term term term-list)
  (let ((exp (order term))
	(len (length term-list)))
    (define (adjoin-iter term times)
      (cond ((empty-termlist? term-list)
	     (list exp))
	    ((= times len)
	     (cons term sopfdjgkl;ajadklsfaaaaaaaaaaaaaaaaaa
(define (first-term term-list)
  (cons  (car term-list)) (- (length term-list) 1))

